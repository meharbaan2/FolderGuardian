<Window x:Class="FolderGuardian.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FolderGuardian" Height="450" Width="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button Name="EncryptBtn" Content="Encrypt Folder" Width="120" Margin="0 0 10 0" Click="EncryptBtn_Click"/>
            <Button Name="DecryptBtn" Content="Decrypt Folder" Width="120" Margin="0 0 10 0" Click="DecryptBtn_Click"/>
            <Button Name="MonitorBtn" Content="Start Monitoring" Width="150" Click="MonitorBtn_Click"/>
        </StackPanel>

        <TextBlock Text="Log:" FontWeight="Bold" Grid.Row="1" Margin="0 0 0 5"/>

        <TextBox Name="LogBox" Grid.Row="2" 
                 TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 IsReadOnly="True"/>

        <Button Content="Clear Log" Grid.Row="3" Width="100" HorizontalAlignment="Right" Margin="0 10 0 0" Click="ClearLog_Click"/>
    </Grid>
</Window>
